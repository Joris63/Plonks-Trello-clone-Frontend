import React from "react";

const AddCardActions = (props) => {
  return (
    <div className="actions_list">
      <button>Members...</button>
      <button>Labels...</button>
    </div>
  );
};

export default AddCardActions;
